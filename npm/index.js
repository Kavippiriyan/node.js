const variable = require("lodash")

variable.each([1,3,5],(item)=>
{
    console.log(item+5);
})

/// lodash is one of the package 
/// -- nodemon is one of the package is used for target one file whenever we are doing update on the file it will run
// automatically

// var a=10
// console.log(a);